# Runtime model assets

This directory contains checked-in model assets used by active vision
regression tests and deployment. It is not a Python source directory.

Models are produced or fine-tuned only through the offline `pretraining`
workspace. Replacing a model is a deliberate runtime change and must include
vision regression verification.
