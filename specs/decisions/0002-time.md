# ADR 0002: UTC and monotonic time

Status: accepted

UTC is used for externally meaningful timestamps. Monotonic seconds are used
for durations, ordering, cooldowns and retries. Core code never waits directly
on wall-clock time, enabling deterministic virtual-time scenarios.
