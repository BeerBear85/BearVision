# Legacy Edge application

This directory contains the unsupported pre-3.0 Edge application, its modules,
manual Box GUI and old test suites, including the former `tools/tools_tests`
scripts. It is retained for historical reference and is excluded from
installation, default tests and CI.

The original contributor guide is preserved as `CLAUDE.md`; its commands and
paths describe the old repository layout and are not active instructions.
