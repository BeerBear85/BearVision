# Legacy code

Code below this directory is retained for historical reference. It is not part
of the BearVision 3 package, supported workflows, default tests or CI gates.

Do not import legacy modules from `src/bearvision`, `apps`, `pretraining` or
active tests. Reuse an idea by reimplementing it behind an active BearVision 3
interface instead of adding a dependency on this tree.
