# Legacy post-processing pipeline

This is the unsupported pre-3.0 virtual-cameraman implementation. Its CLI, GUI,
modules, tests and historical documentation are kept together only for
reference and possible behavioural comparison.

It is intentionally excluded from installation, default test discovery and CI.
The supported implementation is
`src/bearvision/processing/virtual_cameraman.py`.
